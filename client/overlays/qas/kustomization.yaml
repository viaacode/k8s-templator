apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: hetarchief-v3

resources:
  - ../../base
  - client-qas-externalsecret.yaml

generatorOptions:
  disableNameSuffixHash: true

configMapGenerator:
  - name: client-qas-config        # matches <app>-config in envFrom
    envs:
      - client-config-qas.env

