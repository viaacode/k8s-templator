apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: client-qas-vault
spec:
  refreshInterval: 1h
  secretStoreRef:
    name: vault-backend
    kind: ClusterSecretStore
  target:
    name: client-qas-vault
    creationPolicy: Owner
  dataFrom:
    - extract:
        key: "/hetarchief-v3/client-qas"       # VAULT path: hetarchief-v3/client
