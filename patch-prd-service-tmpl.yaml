apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}
  labels:
    env: prd
    component: ${NAMESPACE}
  annotations:
    meemoo.be/kustomized: "true"
    meemoo.be/project: "${NAMESPACE}"
spec:
  selector:
    env: prd
