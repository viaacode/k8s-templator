apiVersion: v1
kind: Service
metadata:
  name: ${APP_NAME}
  labels:
    env: qas
    component: ${NAMESPACE}
  annotations:
    meemoo.be/kustomized: "true"
    meemoo.be/project: "${NAMESPACE}"
spec:
  selector:
    env: qas
